{
  "name": "maggie-stock-ai",
  "version": "0.1.0-rc.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/runtime/server.ts",
    "telegram:set-webhook": "node --experimental-strip-types scripts/setTelegramWebhook.ts",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "test:tsx": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  }
}
